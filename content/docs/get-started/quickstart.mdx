---
title: 'Quickstart'
description: 'Get up and running with the Lumoris Labs API in minutes.'
---

# Quickstart

Get your first image generated in under 2 minutes.

## Step 1 — Start the server

Make sure the API server is running locally:

```bash
node index.js
```

You should see:
```bash
Lumoris Labs API running on https://lumorislabs.online
```

## Step 2 — Authenticate

To make API requests, you must authenticate using a Bearer token.
You can generate a new API Key from your [Dashboard](/dashboard/keys).

All API requests require the \`Authorization\` header:
\`\`\`http
Authorization: Bearer sk-live-lumoris-...
\`\`\`

## Step 3 — Send your first request

```bash
curl -X POST https://api.lumorislabs.online/image-generation/flux1-1-pro \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <YOUR_API_KEY>" \
  -d '{
    "prompt": "a red Ferrari on a mountain road at sunset",
    "aspectRatio": "16:9"
  }'
```

## Step 4 — Get your image URL

```json
{
  "success": true,
  "imageUrl": "https://pre-signed-firefly-prod.s3-accelerate.amazonaws.com/images/..."
}
```

Open the `imageUrl` in your browser to view the generated image!

## Supported Aspect Ratios

| Value  | Label      | Dimensions     |
|--------|------------|----------------|
| `16:9` | Widescreen | 1920 × 1080    |
| `4:3`  | Landscape  | 1152 × 864     |
| `1:1`  | Square     | 1080 × 1080    |
| `3:4`  | Portrait   | 864 × 1152     |
| `9:16` | Vertical   | 1080 × 1920    |
