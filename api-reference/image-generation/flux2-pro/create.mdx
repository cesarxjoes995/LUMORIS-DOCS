---
title: 'FLUX.2 Pro'
api: 'POST /api/image-generation/flux2-pro'
description: 'Submit an image generation job using the FLUX.2 [pro] model.'
---

<Note>
  **FLUX.2 [pro]** — Next generation high-quality model.
</Note>

### Body

<ParamField body="prompt" type="string" required>
  The text description of the image you want to generate.
</ParamField>

<ParamField body="aspectRatio" type="string" default="16:9">
  The aspect ratio of the generated image. This determines the dimensions of the final output. Supported values include:
  - `21:9` (Ultra Wide)
  - `16:9` (Widescreen)
  - `5:4` (Classic)
  - `4:3` (Landscape)
  - `3:2` (Wide)
  - `1:1` (Square)
  - `4:5` (Standard)
  - `3:4` (Portrait)
  - `2:3` (Tall)
  - `9:16` (Vertical)
</ParamField>

### Response

<ResponseField name="jobId" type="string">
  Unique identifier for the generation job. Use this to poll for results.
</ResponseField>

<ResponseField name="status" type="string">
  Initial job status. Always `queued` on creation.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST https://disbelief-recent-thaw.ngrok-free.dev/api/image-generation/flux2-pro \
  -H "Content-Type: application/json" \
  -d '{"prompt": "A futuristic datacenter", "aspectRatio": "16:9"}'
```
</RequestExample>
